# Incrypt Smart Router — optional API keys for paid tiers
# Start with no keys for free-only routing. Add keys to scale up.

# ========== FREE TIER (no keys required) ==========
# Cerebras, HuggingFace Inference, Groq free tiers are used when no keys below.

# ========== STANDARD / LOW-COST ==========
# OPENAI_API_KEY=sk-...
# GOOGLE_AI_API_KEY=...
# ANTHROPIC_API_KEY=sk-ant-...

# ========== PREMIUM (complex tasks) ==========
# Same keys as above; router selects model by task complexity.

# ========== ROUTER CONFIG ==========
PORT=3140
CACHE_TTL_SECONDS=3600
CACHE_MAX_SIZE=1000
SEMANTIC_SIMILARITY_THRESHOLD=0.92
COMPRESSION_ENABLED=true
LOG_LEVEL=info
